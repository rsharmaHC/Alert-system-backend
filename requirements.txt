# =============================================================================
# TM Alert - Full Requirements (CI/CD + Development)
#
# SECURITY: For production deployments, use hash-verified requirements:
#   pip install --require-hashes -r requirements-locked.txt
#
# To regenerate requirements-locked.txt with updated hashes:
#   pip-compile --generate-hashes requirements.txt -o requirements-locked.txt
# =============================================================================

# Core
fastapi==0.135.3
uvicorn[standard]==0.44.0
pydantic==2.12.5
pydantic-settings==2.13.1
python-multipart==0.0.24
python-dotenv==1.0.1

# Database
sqlalchemy==2.0.30
alembic==1.13.1
psycopg2-binary==2.9.9
email-validator==2.1.1

# Cache & Queue
redis==5.0.4
celery==5.4.0

# Authentication
PyJWT==2.12.1
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
pyotp==2.9.0

# External Services
twilio==9.1.0
boto3==1.34.120
aiosmtplib==3.0.1

# Location Services
geopy==2.4.1
shapely==2.0.4

# WebSockets
websockets==12.0
python-socketio==5.14.0

# HTTP Client
httpx==0.27.0

# LDAP Authentication
ldap3==2.9.1

# Data Processing
openpyxl==3.1.3

# Timezone Support
pytz==2024.1

# =============================================================================
# TESTING & QUALITY
# =============================================================================
# Test tooling (pytest, hypothesis, locust, mutmut, ruff, mypy, bandit,
# safety, pip-audit) lives in requirements-test.txt. Production images
# should install requirements-prod.txt only; CI runs
#   pip install -r requirements.txt -r requirements-test.txt
# to get the full dev/test toolchain (security review Dep-L1).
